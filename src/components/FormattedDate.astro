---
const { date } = Astro.props

const formattedDate = new Date(date).toLocaleDateString("en-US", {
  year: "numeric",
  month: "short",
})
---

<time datetime={date} data-title={date}>{formattedDate}</time>
